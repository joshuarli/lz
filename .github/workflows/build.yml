name: Build

on:
  push:
    tags:
      - 'release/*'

permissions:
  contents: write

jobs:
  build:
    name: Build (${{ matrix.runner }})
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 10
    defaults:
      run:
        shell: bash -euo pipefail {0}
    strategy:
      fail-fast: false
      # Native runners required: -Z build-std makes cross-compilation with
      # `cross` impractical (needs nightly toolchain + rust-src inside the
      # container). Native runners let us build and test identically everywhere.
      matrix:
        include:
          - runner: macos-15
            target: aarch64-apple-darwin
          - runner: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - runner: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu

    steps:
      - uses: actions/checkout@v4

      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.runner }}-${{ matrix.target }}

      - name: Run tests
        run: make test-ci

      - name: Build release
        run: make release

      - uses: actions/upload-artifact@v4
        with:
          name: lz-${{ matrix.target }}
          path: target/${{ matrix.target }}/release/lz

  release:
    name: Create release
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/download-artifact@v4

      - name: Create GitHub release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ github.ref_name }}
          GH_REPO: ${{ github.repository }}
        run: |
          V="${VERSION#release/}"
          for dir in lz-*; do
            target="${dir#lz-}"
            mv "$dir/lz" "lz-${V}-${target}"
          done
          gh release create "$VERSION" lz-"${V}"-* --title "$V"
